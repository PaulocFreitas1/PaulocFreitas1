const listaDeNumeros=[25, 88, 32, 40, 101, 97]
const usuarios =['Ana', 'Paulo', 'Murilo', 'Eduarda']

console.log("Print via for \n")
for (const usuario of usuarios){
    console.log(usuario)
}
// o numero ou usuario tanto faz o nome, so usei o elemento com nome parecido para facilitar
for (const numero of listaDeNumeros){
    console.log(numero)
}
